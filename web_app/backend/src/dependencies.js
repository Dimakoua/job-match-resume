// dependencies.js - Dependency injection container

export const dependencies = {
  // Add services, repositories, and other dependencies here
  // Example:
  // signUpService: new SignUpService(userRepository),
  // userRepository: new D1UserRepository(DB),
};